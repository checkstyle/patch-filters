package TreeWalker.CommentsIndentation;

    // violation without filter
public interface Test {
int a = 1;
    // violation without filter
}

    // violation without filter
enum Test2 {
    // violation without filter
NEWLINE

}
